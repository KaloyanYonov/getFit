export default function SubmitButton() {
    return (
        <button
            type="submit"
            className="px-8 py-3 rounded-lg font-semibold text-white bg-gradient-to-b from-blue-300 to-blue-200 
            hover:from-blue-400 hover:to-blue-200 hover:bg-gradient-to-b transition"
        >
            Submit
        </button>
    );
}
